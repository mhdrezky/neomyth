services:
  whisper-stt:
    build: .
    container_name: whisper-stt
    restart: unless-stopped
    ports:
      - "${WHISPER_PORT:-5002}:8000"
    env_file:
      - .env
    volumes:
      - whisper-cache:/root/.cache

volumes:
  whisper-cache:
